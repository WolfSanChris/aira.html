<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FinTech AI Pro - Generación de Ingresos Inteligente</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }

        /* Enhanced animated background */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 25% 25%, rgba(0, 212, 255, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 75% 75%, rgba(255, 215, 0, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 50% 10%, rgba(34, 197, 94, 0.08) 0%, transparent 50%);
            animation: backgroundShift 12s ease-in-out infinite alternate;
            z-index: -2;
        }

        @keyframes backgroundShift {
            0% { transform: scale(1) rotate(0deg); opacity: 0.8; }
            100% { transform: scale(1.2) rotate(3deg); opacity: 1; }
        }

        /* Floating particles with variety */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        .particle {
            position: absolute;
            border-radius: 50%;
            animation: float 15s infinite ease-in-out;
        }

        .particle.type-1 {
            width: 4px;
            height: 4px;
            background: #00d4ff;
        }

        .particle.type-2 {
            width: 3px;
            height: 3px;
            background: #4ade80;
        }

        .particle.type-3 {
            width: 2px;
            height: 2px;
            background: #ffd700;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) translateX(0px); opacity: 0; }
            25% { opacity: 1; }
            50% { opacity: 0.8; }
            100% { transform: translateY(-100vh) translateX(100px); opacity: 0; }
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
            z-index: 1;
        }

        /* Enhanced header with stats */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 25px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            margin-bottom: 30px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 25px;
            backdrop-filter: blur(25px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            animation: slideInDown 0.8s ease-out;
        }

        .logo {
            font-size: 32px;
            font-weight: bold;
            background: linear-gradient(45deg, #00d4ff, #ffd700, #4ade80);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            display: flex;
            align-items: center;
            gap: 15px;
            animation: logoGlow 3s ease-in-out infinite alternate;
        }

        @keyframes logoGlow {
            from { filter: drop-shadow(0 0 15px rgba(0, 212, 255, 0.6)); }
            to { filter: drop-shadow(0 0 25px rgba(255, 215, 0, 0.7)); }
        }

        .header-stats {
            display: flex;
            gap: 30px;
            align-items: center;
        }

        .stat-item {
            text-align: center;
            padding: 15px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            min-width: 120px;
        }

        .stat-value {
            font-size: 24px;
            font-weight: bold;
            color: #4ade80;
            margin-bottom: 5px;
        }

        .stat-label {
            font-size: 12px;
            color: #666;
        }

        /* Enhanced dashboard grid */
        .dashboard {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 30px;
            margin-bottom: 30px;
        }

        .left-panel {
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        .right-panel {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .card {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 25px;
            padding: 30px;
            backdrop-filter: blur(25px);
            position: relative;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            animation: slideInUp 0.6s ease-out forwards;
            opacity: 0;
        }

        .card:nth-child(1) { animation-delay: 0.1s; }
        .card:nth-child(2) { animation-delay: 0.2s; }
        .card:nth-child(3) { animation-delay: 0.3s; }
        .card:nth-child(4) { animation-delay: 0.4s; }
        .card:nth-child(5) { animation-delay: 0.5s; }

        @keyframes slideInUp {
            from { transform: translateY(50px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .card:hover {
            transform: translateY(-8px) scale(1.02);
            border-color: #00d4ff;
            box-shadow: 0 25px 50px rgba(0, 212, 255, 0.25);
        }

        .card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: conic-gradient(transparent, rgba(0, 212, 255, 0.1), transparent);
            animation: rotate 6s linear infinite;
            z-index: -1;
        }

        @keyframes rotate {
            100% { transform: rotate(360deg); }
        }

        /* Enhanced balance display */
        .balance-card {
            background: linear-gradient(135deg, rgba(0, 212, 255, 0.1), rgba(255, 215, 0, 0.1));
            border: 2px solid rgba(255, 215, 0, 0.3);
            text-align: center;
            position: relative;
        }

        .balance-amount {
            font-size: 56px;
            font-weight: bold;
            background: linear-gradient(45deg, #00d4ff, #ffd700, #4ade80);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin: 25px 0;
            animation: balanceGlow 4s ease-in-out infinite alternate;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .balance-amount:hover {
            transform: scale(1.08);
            filter: drop-shadow(0 0 40px rgba(255, 215, 0, 0.9));
        }

        @keyframes balanceGlow {
            from { filter: drop-shadow(0 0 25px rgba(0, 212, 255, 0.6)); }
            to { filter: drop-shadow(0 0 45px rgba(255, 215, 0, 0.9)); }
        }

        .balance-metrics {
            display: flex;
            justify-content: space-around;
            margin-top: 25px;
            gap: 20px;
        }

        .metric-item {
            text-align: center;
            flex: 1;
            padding: 15px;
            background: rgba(0, 0, 0, 0.2);
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .metric-value {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .metric-label {
            font-size: 12px;
            color: #999;
        }

        /* Enhanced AI income generator */
        .ai-generator {
            position: relative;
        }

        .ai-status-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding: 15px;
            background: rgba(0, 0, 0, 0.3);
            border-radius: 15px;
            border: 1px solid rgba(0, 212, 255, 0.3);
        }

        .ai-status {
            display: flex;
            align-items: center;
            gap: 10px;
            color: #00d4ff;
        }

        .ai-indicator {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #4ade80;
            animation: aiPulse 2s infinite;
        }

        @keyframes aiPulse {
            0%, 100% { opacity: 1; transform: scale(1); }
            50% { opacity: 0.5; transform: scale(1.2); }
        }

        .earning-rate {
            color: #4ade80;
            font-weight: bold;
            font-size: 18px;
        }

        .opportunities-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-bottom: 25px;
        }

        .opportunity-card {
            padding: 20px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .opportunity-card:hover {
            transform: translateY(-3px);
            border-color: #00d4ff;
            background: rgba(0, 212, 255, 0.05);
        }

        .opportunity-card.active {
            border-color: #4ade80;
            background: rgba(74, 222, 128, 0.1);
            animation: opportunityPulse 2s infinite;
        }

        @keyframes opportunityPulse {
            0%, 100% { box-shadow: 0 0 0 0 rgba(74, 222, 128, 0.4); }
            50% { box-shadow: 0 0 0 10px rgba(74, 222, 128, 0); }
        }

        .opportunity-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }

        .opportunity-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            margin-bottom: 10px;
        }

        .opportunity-title {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .opportunity-desc {
            font-size: 12px;
            color: #999;
            margin-bottom: 10px;
        }

        .opportunity-reward {
            font-size: 16px;
            font-weight: bold;
            text-align: center;
            padding: 8px;
            background: rgba(0, 0, 0, 0.3);
            border-radius: 8px;
        }

        .progress-bar {
            width: 100%;
            height: 4px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 2px;
            overflow: hidden;
            margin-top: 10px;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #4ade80, #22c55e);
            border-radius: 2px;
            transition: width 0.3s ease;
        }

        /* Enhanced control buttons */
        .control-panel {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }

        .btn {
            padding: 15px 25px;
            border: none;
            border-radius: 12px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            transform: translate(-50%, -50%);
            transition: all 0.6s ease;
        }

        .btn:active::before {
            width: 300px;
            height: 300px;
        }

        .btn-primary {
            background: linear-gradient(45deg, #00d4ff, #0ea5e9);
            color: #fff;
            box-shadow: 0 6px 20px rgba(0, 212, 255, 0.4);
        }

        .btn-success {
            background: linear-gradient(45deg, #4ade80, #22c55e);
            color: #fff;
            box-shadow: 0 6px 20px rgba(74, 222, 128, 0.4);
        }

        .btn-warning {
            background: linear-gradient(45deg, #fbbf24, #f59e0b);
            color: #000;
            box-shadow: 0 6px 20px rgba(251, 191, 36, 0.4);
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(0, 0, 0, 0.3);
        }

        /* Real-time stats sidebar */
        .stats-sidebar {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 20px;
            padding: 25px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .stats-header {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 20px;
            color: #00d4ff;
        }

        .stat-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .stat-row:last-child {
            border-bottom: none;
        }

        .stat-name {
            font-size: 14px;
            color: #ccc;
        }

        .stat-value-live {
            font-weight: bold;
            font-size: 16px;
        }

        /* Live activity feed */
        .activity-feed {
            max-height: 400px;
            overflow-y: auto;
        }

        .activity-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 12px;
            margin-bottom: 8px;
            background: rgba(255, 255, 255, 0.02);
            border-radius: 10px;
            border-left: 3px solid transparent;
            transition: all 0.3s ease;
            animation: slideInRight 0.5s ease-out;
        }

        @keyframes slideInRight {
            from { transform: translateX(50px); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }

        .activity-item.income {
            border-left-color: #4ade80;
        }

        .activity-item.processing {
            border-left-color: #00d4ff;
        }

        .activity-item.completed {
            border-left-color: #ffd700;
        }

        .activity-icon {
            width: 35px;
            height: 35px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            flex-shrink: 0;
        }

        .activity-info {
            flex: 1;
        }

        .activity-title {
            font-size: 13px;
            font-weight: 500;
            margin-bottom: 2px;
        }

        .activity-time {
            font-size: 11px;
            color: #666;
        }

        .activity-amount {
            font-weight: bold;
            font-size: 14px;
        }

        /* Enhanced notifications */
        .notification {
            position: fixed;
            top: 20px;
            right: 20px;
            padding: 20px;
            border-radius: 15px;
            color: #fff;
            font-weight: 500;
            opacity: 0;
            transform: translateX(400px) scale(0.8);
            transition: all 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            z-index: 1001;
            max-width: 400px;
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .notification.show {
            opacity: 1;
            transform: translateX(0) scale(1);
        }

        .notification.success {
            background: linear-gradient(135deg, #4ade80, #22c55e);
            box-shadow: 0 15px 35px rgba(74, 222, 128, 0.4);
        }

        .notification.info {
            background: linear-gradient(135deg, #00d4ff, #0ea5e9);
            box-shadow: 0 15px 35px rgba(0, 212, 255, 0.4);
        }

        .notification.warning {
            background: linear-gradient(135deg, #fbbf24, #f59e0b);
            box-shadow: 0 15px 35px rgba(251, 191, 36, 0.4);
        }

        /* Money animation effects */
        .money-burst {
            position: fixed;
            font-size: 20px;
            color: #4ade80;
            z-index: 1002;
            animation: moneyBurst 2s ease-out forwards;
            pointer-events: none;
        }

        @keyframes moneyBurst {
            0% {
                transform: scale(0) rotate(0deg);
                opacity: 1;
            }
            50% {
                transform: scale(1.5) rotate(180deg);
                opacity: 0.8;
            }
            100% {
                transform: scale(0.5) rotate(360deg) translateY(-200px);
                opacity: 0;
            }
        }

        /* Loading states */
        .loading-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.8);
            display: none;
            align-items: center;
            justify-content: center;
            border-radius: 25px;
            z-index: 10;
            backdrop-filter: blur(5px);
        }

        .loading-content {
            text-align: center;
        }

        .loading-spinner {
            width: 60px;
            height: 60px;
            border: 4px solid rgba(0, 212, 255, 0.3);
            border-top: 4px solid #00d4ff;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-bottom: 15px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Responsive design */
        @media (max-width: 1200px) {
            .dashboard {
                grid-template-columns: 1fr;
            }
            
            .opportunities-grid {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 768px) {
            .header-stats {
                flex-direction: column;
                gap: 15px;
            }
            
            .balance-amount {
                font-size: 40px;
            }
            
            .opportunities-grid {
                grid-template-columns: 1fr;
            }
            
            .control-panel {
                flex-direction: column;
            }
            
            .btn {
                width: 100%;
                justify-content: center;
            }
        }

        /* Special achievement effects */
        .achievement-badge {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) scale(0);
            background: linear-gradient(45deg, #ffd700, #ffed4e);
            color: #000;
            padding: 30px;
            border-radius: 20px;
            text-align: center;
            z-index: 1003;
            border: 3px solid #ffd700;
            box-shadow: 0 0 50px rgba(255, 215, 0, 0.5);
            animation: achievementPop 3s ease-out forwards;
        }

        @keyframes achievementPop {
            0% { transform: translate(-50%, -50%) scale(0) rotate(-180deg); opacity: 0; }
            20% { transform: translate(-50%, -50%) scale(1.2) rotate(0deg); opacity: 1; }
            90% { transform: translate(-50%, -50%) scale(1) rotate(0deg); opacity: 1; }
            100% { transform: translate(-50%, -50%) scale(0) rotate(180deg); opacity: 0; }
        }

        .achievement-title {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .achievement-desc {
            font-size: 16px;
            margin-bottom: 15px;
        }

        .achievement-reward {
            font-size: 20px;
            font-weight: bold;
            color: #059669;
        }
    </style>
</head>
<body>
    <!-- Floating Particles -->
    <div class="particles" id="particles"></div>

    <div class="container">
        <!-- Enhanced Header -->
        <div class="header">
            <div class="logo">
                <i class="fas fa-brain"></i>
                FinTech AI Pro
            </div>
            <div class="header-stats">
                <div class="stat-item">
                    <div class="stat-value" id="totalUsers">1,247</div>
                    <div class="stat-label">Usuarios Activos</div>
                </div>
                <div class="stat-item">
                    <div class="stat-value" id="totalEarned">$47.2K</div>
                    <div class="stat-label">Generado Hoy</div>
                </div>
                <div class="stat-item">
                    <div class="stat-value">99.8%</div>
                    <div class="stat-label">Uptime IA</div>
                </div>
            </div>
        </div>

        <div class="dashboard">
            <!-- Left Panel -->
            <div class="left-panel">
                <!-- Enhanced Balance Card -->
                <div class="card balance-card">
                    <h3><i class="fas fa-wallet"></i> Balance Total</h3>
                    <div class="balance-amount" id="totalBalance" onclick="celebrateBalance()">$2,847.50</div>
                    
                    <div class="balance-metrics">
                        <div class="metric-item">
                            <div class="metric-value" style="color: #4ade80;" id="todayEarnings">+$127.30</div>
                            <div class="metric-label">Hoy</div>
                        </div>
                        <div class="metric-item">
                            <div class="metric-value" style="color: #00d4ff;" id="weekEarnings">+$892.15</div>
                            <div class="metric-label">Esta Semana</div>
                        </div>
                        <div class="metric-item">
                            <div class="metric-value" style="color: #ffd700;" id="monthEarnings">+$3,247.80</div>
                            <div class="metric-label">Este Mes</div>
                        </div>
                    </div>
                </div>

                <!-- Enhanced AI Income Generator -->
                <div class="card ai-generator">
                    <div class="loading-overlay" id="aiLoading">
                        <div class="loading-content">
                            <div class="loading-spinner"></div>
                            <div>IA analizando mejores oportunidades...</div>
                        </div>
                    </div>
                    
                    <h3><i class="fas fa-robot"></i> Generador de Ingresos IA</h3>
                    
                    <div class="ai-status-bar">
                        <div class="ai-status">
                            <div class="ai-indicator"></div>
                            <span id="aiStatusText">IA Activa • Escaneando mercados</span>
                        </div>
                        <div class="earning-rate">
                            <i class="fas fa-arrow-up"></i>
                            <span id="earningRate">$4.20/min</span>
                        </div>
                    </div>

                    <div class="opportunities-grid" id="opportunitiesGrid">
                        <!-- Opportunities will be dynamically added here -->
                    </div>

                    <div class="control-panel">
                        <button class="btn btn-success" onclick="startAIEarning()" id="aiStartBtn">
                            <i class="fas fa-play"></i> Iniciar IA
                        </button>
                        <button class="btn btn-warning" onclick="pauseAIEarning()" id="aiPauseBtn" disabled>
                            <i class="fas fa-pause"></i> Pausar
                        </button>
                        <button class="btn btn-primary" onclick="boostAIEarning()" id="aiBoostBtn">
                            <i class="fas fa-rocket"></i> Boost x2
                        </button>
                    </div>
                </div>
            </div>

            <!-- Right Panel -->
            <div class="right-panel">
                <!-- Real-time Stats -->
                <div class="card stats-sidebar">
                    <div class="stats-header">
                        <i class="fas fa-chart-line"></i>
                        <h4>Estadísticas en Tiempo Real</h4>
                    </div>
                    
                    <div class="stat-row">
                        <span class="stat-name">Tasa de Éxito IA</span>
                        <span class="stat-value-live" style="color: #4ade80;" id="successRate">94.7%</span>
                    </div>
                    <div class="stat-row">
                        <span class="stat-name">Oportunidades Activas</span>
                        <span class="stat-value-live" style="color: #00d4ff;" id="activeOpportunities">12</span>
                    </div>
                    <div class="stat-row">
                        <span class="stat-name">Tiempo Promedio</span>
                        <span class="stat-value-live" style="color: #ffd700;" id="avgTime">3.4 min</span>
                    </div>
                    <div class="stat-row">
                        <span class="stat-name">Próximo Pago</span>
                        <span class="stat-value-live" style="color: #a855f7;" id="nextPayout">2:34</span>
                    </div>
                </div>

                <!-- Live Activity Feed -->
                <div class="card">
                    <div class="stats-header">
                        <i class="fas fa-activity"></i>
                        <h4>Actividad Reciente</h4>
                    </div>
                    
                    <div
